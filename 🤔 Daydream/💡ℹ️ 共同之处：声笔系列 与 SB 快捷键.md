#Info #💡

# 💡ℹ️ 填字游戏：互斥地填满所有格子

快捷键表格 [[CONFIG/KeyBinding]] 在膨胀。
剩下的可分配的快捷键空位，已经不多了。

这些快捷键应该 如何分配给其余有用的功能函数？

这类似于 [[Language/Input Method/声笔飞单]]/码 中的 缩减码 中，
对 笔画（如 声笔(笔) 字/词）
以及 标点符号 [[Language/Input Method/声笔飞单#==标bme点db ~ SD==]] 的 极限利用：
为了 不打破规则 而 单独制定规则。
