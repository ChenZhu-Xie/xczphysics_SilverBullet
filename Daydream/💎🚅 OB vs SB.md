
# Obsidian vs SilverBullet

1. [BV1gfa8zTEvq](https://www.bilibili.com/video/BV1gfa8zTEvq?t=252.0) #bilibili

OB 是通过 增加 锚文本 ^,并通过 `[[^^]]` 来全文检索它，
以实现 细粒度链接的。
并且也只精确到行，而不是光标（当然，OB 的插件应该容易实现这一点）

## 文件系统 + 解析 > Tana 纯数据库 的一个体现

SY + OB = SB -_-|| 都只是 尝试去 解析 .md 文件
因此 同一份这些 .md 文件 既可以同时被 它们仨读取，
也可以 被其他的 IDE 编辑
也可以 被神奇的 MCP 操作： [BV1gfa8zTEvq](https://www.bilibili.com/video/BV1gfa8zTEvq?t=350.2) #bilibili

# 其他 基本操作

## 列表

OB 和 SiYuan 都 支持 - 创建 Bullets

## 前进后退

SiYuan 的 前进后退，默认 采用的是 `Ctrl + []`
OB 的前进后退，还没找到
- 既不是 SY 的，也不是 浏览器/SB 的 `Alt+<>`
